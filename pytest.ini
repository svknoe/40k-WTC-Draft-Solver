[pytest]
# Slow tests (e.g. the Scotland 8-player k=3 golden value, ~5 minutes) are
# deselected by default so `pytest` stays CI-friendly. Run them explicitly
# with: pytest -m slow
addopts = -m "not slow"
markers =
    slow: marks tests as slow (deselected by default; run with -m slow)
