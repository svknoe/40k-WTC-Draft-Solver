{
  "name": "wtc-draft-solver-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser engine + trainer for the WTC draft solver (issue #17 spike: TS engine core, conformance suite, benchmark harness)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
